import React from 'react';
import IBatch from '../../model/IBatch';

export interface IBatchProps {
    createData: (body: IBatch) => void,

}

const rows = [
];




export const BatchTableComponent: React.FC<any> = (props: IBatchProps) => {



return (
<>
</>

)

}